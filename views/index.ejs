<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Url Shortner</title>
    <style>
        body {
            font-family: Arial, sans-serif;
        }
        h1 {
            text-align: center;
        }
       .url-form {
        width: 300px;
            margin: 0 auto;
            padding: 20px;
            border-radius: 4px;
            background-color: #f8f9fa;
            border: 1px solid #0b5cad;
            box-shadow: 0 1px 1px rgba(0, 0, 0, 0.05);
            margin-top: 20px;
            margin-bottom: 20px;
        }
        .url-form label {
        display: block;
            margin-bottom: 5px;
        }
        .url-form input[type="text"] {
            width: 90%;
            padding: 10px;
            border: 2px solid black;
            border-radius: 5px;
            margin-bottom: 15px;
        }
        .url-form input[type="submit"] {
            padding: 10px 20px;
            border-radius: 5px;
            transition: background-color 0.3s ease;
            border-color: #007bff;
        }
       .shorten-urls {
        width: 800px;
            margin: 0 auto;
            padding: 20px;
            border-radius: 4px;
            background-color: #fff;
            border: 1px solid #ced4da;
            box-shadow: 0 1px 1px rgba(0, 0, 0, 0.05);

        }
       .shorten-urls li {
        list-style-type: none;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 10px;
            border-bottom: 1px solid #ddd;
        }
       .shorten-urls li a {
        color: #007bff;
            text-decoration: none;
        }
        a:hover {
            color: #0056b3;
        }
        .shorten-urls h2 {
        text-align: center;
        }
        .navbar{
            background-color: blue;
            color: #fff;
            display: flex;
            justify-content: space-between;
            padding: 10px;
            box-shadow: 0 1px 1px rgba(0, 0, 0, 0.05);
            height: 30px;
            position: sticky;
            border-bottom-right-radius: 4px;
            border-bottom-left-radius: 4px;
            border-bottom: 2px solid black;
        }
        .profile{
            display: flex;
            align-items: center;
            margin-left: 10px;
            margin-right: 10px;
        }
        .profile h3{
            margin-right: 10px;
            font-weight: bold;
            cursor: pointer;
        }
        .profile img{
            width: 30px;
            height: 30px;
            object-fit: cover;
            border: 2px solid #fff;
            margin-right: 10px;
            border-radius: 50%;
            cursor: pointer;
        }
        nav input{
            padding: 10px;
            border: 2px solid black;
            border-radius: 5px;
            margin-right: 10px;
            margin-left: 10px;
            transition: background-color 0.3s ease;
            border-color: #007bff;
            min-width: 200px;
            max-width: 300px;
        }
        nav button{
            padding: 10px 20px;
            margin-left: 10px;
            margin-right: 5px;
            border-radius: 5px;
            transition: background-color 0.3s ease;
            border-color: #007bff;
             cursor: pointer;
        }
        .website{
            display: flex;
            align-items: center;
            margin-right: 10px;
            margin-left: 10px;
        }
        .website h2{
            margin-right: 10px;
            font-weight: bold;
        }


    </style>
    
</head>
<body>
    <!-- Navigation bar or header goes here -->
    <nav class="navbar">
            <!-- Navigation links goes here -->
         
             <!-- Logo or brand name goes here -->
             <div class="website">
                <h2>URL Shortener</h2>
             </div>
             <!-- Search bar goes here -->
             <input type="text" placeholder="Search...">
             <div class="profile">
                 <!-- Profile image goes here -->
                 <img src="profile.jpg" alt=" ">
                 <!-- User's name goes here -->
                  <h3> Hi, <%= user %> </h3>
                  <!-- Log out button -->
                  <button onclick="handleLogOut()">Logout</button>
             </div>
             
    </nav>
    <!-- Main content goes here -->
    <div class="url-form">
        <form action="/url" method="post">
            <label for="url">Enter a URL:</label>
            <input type="text" id="url" name="url" placeholder="https://www.example.com" required>
            <input type="submit" value="Shorten">
        </form>
    </div>
    <% if (locals.id) { %>
        <h2>Your shortened URL is:</h2>
        <p>http://localhost:8001/url/<%= id %></p>
     
    <% } else { %>
    <div class="shorten-urls">
       <h2>Shortened URLs</h2>
        <% urls.forEach(url => { %>
        
            <li><%= url.shortUrl %> - <%= url.redirectURL  %>
                <a href="http://localhost:8001/url/<%= url.shortUrl %>">visit</a>
                <button onclick="handleDelete()" class="delete-url">Delete</button>
            </li>
            
        <% }) %>

        <% } %>
        
         
   </div>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script>
        function handleDelete() {
            // Logic to delete URL from the database and the frontend
            //...
            


        }
        function handleLogOut(){
            // Logic to log out user and redirect to home page
            //...
            window.location.href = '/user/logout';
        }
        
    </script>
    
</body>
</html>